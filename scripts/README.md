# Optional scripts

Add small, repeatable helpers here when a manual step becomes annoying—for example, preparing a reference-image contact sheet, checking dimensions, or renaming exported wallpapers consistently.

Provider API clients are intentionally absent. Add one only after explicitly choosing separate API billing and documenting its local setup.
