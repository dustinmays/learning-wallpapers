# Topic prompts

Store one Markdown file per wallpaper topic. Use a durable, descriptive filename such as `sse-vs-websockets-vs-mqtt.md` and start from `_template.md`.

Keep the source facts separate from visual direction. That makes it easier to redesign a wallpaper without accidentally changing its meaning.
