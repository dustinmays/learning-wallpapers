# Topic prompts

Store one Markdown file per learning-reference topic. Use a durable, descriptive filename such as `sse-vs-websocket-vs-mqtt.md` and start from `_template.md`.

Keep source facts separate from exact wording and presentation direction. This lets a future session derive a document, wallpaper, or both without accidentally changing the meaning.
